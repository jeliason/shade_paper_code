# HPC Connection Settings
# Copy this file to .env and fill in your actual values
# .env is gitignored and should contain your real credentials

# SSH host alias (must be configured in ~/.ssh/config)
HPC_HOST=hpc

# Your username on the HPC
HPC_USER=username

# Remote directory where code will be synced
# This should be an absolute path on the HPC
HPC_DIR=/home/username/shade_paper_code

# Email for SLURM job notifications
HPC_EMAIL=your.email@example.com

# Compiler toolchain module (if your HPC uses modules)
HPC_COMPILER_TOOLCHAIN=gcc/11.2.0

# R module name to load (if your HPC uses modules)
# Leave as "R" if no specific version needed
HPC_R_MODULE=R/4.3.0

# SHADE package source
# Options:
#   - github:username/repo (install from GitHub)
#   - /path/to/local/SHADE (install from local directory)
SHADE_SOURCE=github:yourusername/SHADE

# Data directory path on HPC
# This is where simulation results and large data files will be written
# Can be absolute path or relative to HPC_DIR
# Example: /scratch/username/shade_data or ./data
HPC_DATA_PATH=/scratch/username/shade_data

# Rsync exclusion patterns (comma-separated)
# These paths/patterns will NOT be synced to HPC
RSYNC_EXCLUDE=.git,*.rds,*.json,data/,logs/,.Rproj.user,*.Rproj,.DS_Store,manuscript/
